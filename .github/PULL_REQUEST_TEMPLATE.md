## What changed

Briefly describe the change and why.

## How was this tested

- [ ] `cd gs-back && ./mvnw test` passes
- [ ] `cd gs-front && npx ng test --watch=false --browsers=ChromeHeadless` passes
- [ ] `cd gs-front && npx ng build --configuration production --progress=false` succeeds
- [ ] Manually verified in the browser (describe the flow you checked)

## Related issue

Closes #

## Checklist

- [ ] Docs under `docs/` updated if behavior/config changed
- [ ] No secrets or real credentials included in the diff
